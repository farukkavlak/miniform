{
  "name": "@miniform/graph",
  "version": "1.0.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "esbuild --sourcemap --bundle --platform=node --target=node18 --packages=bundle --outfile=dist/index.js ./src/index.ts",
    "test": "vitest run --coverage",
    "type:check": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.7.0"
  }
}