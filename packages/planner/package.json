{
  "name": "@miniform/planner",
  "version": "1.0.0",
  "main": "src/index.ts",
  "scripts": {
    "build": "esbuild --sourcemap --bundle --platform=node --target=node18 --packages=bundle --outfile=dist/index.js ./src/index.ts",
    "lint": "eslint .",
    "test": "vitest run --coverage",
    "type:check": "tsc --noEmit"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^0.34.0",
    "eslint": "^8.0.0",
    "typescript": "^5.0.0",
    "vitest": "^0.34.0"
  }
}